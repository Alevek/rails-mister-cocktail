
<div class="show-banner">
  <div class="container">
    <h1 class="title"> <%= "##{@cocktail.name}" %></h1>
      <ul class="list-group">
        <% @cocktail.doses.each do |dose| %>
          <li class="list-group-item doses d-flex justify-content-between"><%= dose.ingredient.name %>
            <%= dose.description %>
            <%= link_to "Suppr",
                dose_path(dose),
                method: :delete,
                data: { confirm: "Êtes vous sur?" } %>
            </li>
        <% end %>
      </ul>
      <span><%= link_to 'add dose',  new_cocktail_dose_path(@cocktail) %></span>
  </div>
</div>
